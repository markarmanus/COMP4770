gameState = function() {
    var self = {};

    self.update = function() {
            // this should call all the relevant systems in the gamestate

        render();
        console.log("test");
    }

    self.render = function()) {

    }

    return self;
}